<Window
    x:Class="CleanAimTracker.SummaryWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CleanAimTracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Session Summary">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#1E1E1E">
        <StackPanel Margin="20" VerticalAlignment="Top">

            <TextBlock Text="Session Summary"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0 0 0 20"/>

            <TextBlock x:Name="TimeText" Foreground="LightGray" FontSize="18"/>
            <TextBlock x:Name="DistanceText" Foreground="LightGray" FontSize="18"/>
            <TextBlock x:Name="AvgSpeedText" Foreground="LightGray" FontSize="18"/>
            <TextBlock x:Name="PeakSpeedText" Foreground="LightGray" FontSize="18"/>
            <TextBlock x:Name="FlicksText" Foreground="LightGray" FontSize="18"/>
            <TextBlock x:Name="SmoothnessText" Foreground="LightGray" FontSize="18"/>
            <TextBlock x:Name="SharpnessText" Foreground="LightGray" FontSize="18"/>
            <TextBlock x:Name="ConsistencyText" Foreground="LightGray" FontSize="18"/>

            <TextBlock x:Name="QualityText"
                       Foreground="White"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0 10 0 0"/>
        </StackPanel>
    </ScrollViewer>
</Window>